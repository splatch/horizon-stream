export enum AlertType {
  CRITICAL,
  MAJOR,
  MINOR,
  WARNING,
  INDETERMINATE
}

export const enum AlertStatus {
  ACKNOWLEDGED,
  UNACKNOWLEDGED
}

export const enum TimeType {
  ALL,
  TODAY,
  DAY,
  SEVEN_DAY
}
