export const enum AlertType {
  CRITICAL,
  MAJOR,
  MINOR,
  WARNING,
  INTERMITTENT
}

export const enum AlertStatus {
  ACKNOWLEDGED,
  UNACKNOWLEDGED
}

export const enum AlertSort {
  DATE,
  TIME,
  NODE,
  WATCHING
}
