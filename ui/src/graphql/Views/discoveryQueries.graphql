query ListLocationsForDiscovery {
  ...LocationsParts
}

query ListDiscoveries {
  listActiveDiscovery {
    id
    configName
    ipAddresses
    location
    snmpConfig {
      ports
      readCommunities
    }
  }
  passiveDiscoveries {
    id
    location
    name
    snmpCommunities
    snmpPorts
    toggle
  }
}
