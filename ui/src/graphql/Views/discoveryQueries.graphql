query ListLocationsForDiscovery {
  ...LocationsParts
}

query ListDiscoveryConfig {
  listDiscoveryConfig {
    configName
    ipAddresses
    retries
    snmpConfig {
      ports
      readCommunities
    }
    timeout
  }
}
