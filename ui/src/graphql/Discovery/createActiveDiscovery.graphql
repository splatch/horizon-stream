mutation CreateActiveDiscovery($activeDiscovery: CreateDiscoveryConfigRequestInput!) {
  createActiveDiscovery(request: $activeDiscovery) {
    configName
    ipAddresses
    location
    snmpConfig {
      ports
      readCommunities
    }
  }
}
