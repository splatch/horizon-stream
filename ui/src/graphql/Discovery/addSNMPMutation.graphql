mutation CreateDiscoveryConfig($snmpInfo: CreateDiscoveryConfigRequestInput!) {
  createDiscoveryConfig(request: $snmpInfo) {
    configName
    ipAddresses
    snmpConfig {
      ports
      readCommunities
    }
  }
}
