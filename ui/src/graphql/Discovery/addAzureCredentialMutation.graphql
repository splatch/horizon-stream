mutation CreateAzureActiveDiscovery($discovery: AzureActiveDiscoveryCreateInput!) {
  createAzureActiveDiscovery(discovery: $discovery) {
    createTimeMsec
    locationId
    subscriptionId
    clientId
  }
}
