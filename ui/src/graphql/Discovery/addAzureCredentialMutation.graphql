mutation AddAzureCredential ($azureCredential: AzureCredentialCreateInput!) {
  addAzureCredential (azureCredential: $azureCredential) {
    createTimeMsec
    location
    subscriptionId
    clientId
  }
}
