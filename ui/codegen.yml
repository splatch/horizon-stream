overwrite: true
schema: "http://localhost:9090/graphql"
# schema: "http://localhost:4000/graphql"
documents: './src/graphql/**/*.graphql'
generates:
  src/types/graphql.ts:
  # src/types/appliances.codegen.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"

