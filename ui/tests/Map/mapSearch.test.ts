import MapSearch from '@/components/Map/MapSearch.vue'
import setupWrapper from 'tests/setupWrapper'

let wrapper: any

beforeEach(() => {
  wrapper = setupWrapper({
    component: MapSearch
  })
})

test.todo('...', () => {
  // assert search method has been called correctly
  expect(false).toBeTruthy()
})