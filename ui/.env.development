# IMPORTANT: Do not modify this file for local development, prevent accidentally committing changes to these defaults.
# Instead, create a new file called `.env.development.local`. Vite will use values in there as overrides.
#
# Use this as a start:
#
# #VITE_BASE_URL=http://localhost:4000 # GraphQL mock server (./mock-graphql)
# #VITE_BASE_URL=http://localhost:48080
# #VITE_BASE_URL=http://localhostapi
# #VITE_BASE_URL=http://873b23b8-8c95-44ae-9889-7c0d0018bbe7.mock.pstmn.io # Postman mock server
# #VITE_KEYCLOAK_URL=http://localhostkey

VITE_SERVER_PORT=3000
VITE_BASE_URL=http://localhost:13080
VITE_KEYCLOAK_URL=http://localhost:26080
VITE_KEYCLOAK_REALM=opennms
VITE_KEYCLOAK_CLIENT_ID=horizon-stream
