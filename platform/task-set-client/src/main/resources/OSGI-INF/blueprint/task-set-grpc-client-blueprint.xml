<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.3.0"
           xmlns:ext="http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.5.0"
           xsi:schemaLocation="
		http://www.osgi.org/xmlns/blueprint/v1.0.0
		https://osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
		http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.3.0
		http://aries.apache.org/schemas/blueprint-cm/blueprint-cm-1.3.0.xsd
		http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.5.0
		http://aries.apache.org/schemas/blueprint-ext/blueprint-ext-1.5.xsd
">

    <cm:property-placeholder id="serverProperties" persistent-id="org.opennms.core.taskset.client"
                             update-strategy="reload">
        <cm:default-properties>
            <cm:property name="host" value="localhost"/>
            <cm:property name="port" value="8990"/>
            <cm:property name="tlsEnabled" value="false"/>
            <cm:property name="maxMessageSize" value="10485760"/>
        </cm:default-properties>
    </cm:property-placeholder>

    <bean id="taskSetGrpcClient" class="org.opennms.horizon.core.taskset.client.TaskSetGrpcClient" init-method="init">
        <property name="host" value="${host}"/>
        <property name="port" value="${port}"/>
        <property name="tlsEnabled" value="${tlsEnabled}"/>
        <property name="maxMessageSize" value="${maxMessageSize}"/>
    </bean>

    <service ref="taskSetGrpcClient" interface="org.opennms.taskset.service.api.TaskSetPublisher">
        <service-properties>
            <entry key="type" value="grpc"/>
        </service-properties>
    </service>

</blueprint>
