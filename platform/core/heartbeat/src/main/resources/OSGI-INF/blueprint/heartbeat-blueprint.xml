<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.3.0"
           xmlns:ext="http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.5.0"

           xsi:schemaLocation="
		http://www.osgi.org/xmlns/blueprint/v1.0.0
		https://osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd

		http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.3.0
		http://aries.apache.org/schemas/blueprint-cm/blueprint-cm-1.3.0.xsd

		http://camel.apache.org/schema/blueprint
		http://camel.apache.org/schema/blueprint/camel-blueprint.xsd
">

    <ext:property-placeholder placeholder-prefix="$[" placeholder-suffix="]"/>

    <cm:property-placeholder id="monitorProperties"
                             persistent-id="org.opennms.core.heartbeat"
                             update-strategy="reload">
        <cm:default-properties>
            <cm:property name="ignite.useKubernetes" value="true"/>
            <cm:property name="ignite.kubernetesServiceName" value="horizon-stream-ignite-core"/>

            <cm:property name="heartbeat.kafka-topic" value="heartbeat"/>
            <cm:property name="kafkaBrokers" value="$[env:KAFKA_BROKER_HOST]:$[env:KAFKA_BROKER_PORT]"/>
        </cm:default-properties>
    </cm:property-placeholder>

    <reference id="minionDao" interface="org.opennms.horizon.db.dao.api.MinionDao"/>
    <reference id="sessionUtils" interface="org.opennms.horizon.db.dao.api.SessionUtils" />
    <reference id="metricsAdapter" interface="org.opennms.horizon.metrics.api.OnmsMetricsAdapter"/>
    <reference id="eventForwarder" interface="org.opennms.horizon.events.api.EventForwarder" availability="optional"/>

    <bean id="heartbeatConsumer" class="org.opennms.horizon.core.heartbeat.HeartbeatConsumer" init-method="init">
        <argument ref="minionDao"/>
        <argument ref="sessionUtils"/>
        <argument ref="metricsAdapter"/>
        <argument ref="eventForwarder"/>
    </bean>

    <bean id="heartbeatProcessor" class="org.opennms.horizon.core.heartbeat.camel.HeartbeatProcessor">
        <property name="heartbeatConsumer" ref="heartbeatConsumer"/>
    </bean>

    <camelContext autoStartup="true" id="monitorCamelContext" xmlns="http://camel.apache.org/schema/blueprint"
                  allowUseOriginalMessage="false">
        <propertyPlaceholder id="properties" location="blueprint:monitorProperties"/>

        <route id="consumeKafkaHeartbeats">
            <from uri="kafka:{{heartbeat.kafka-topic}}?brokers={{kafkaBrokers}}&amp;valueDeserializer=org.apache.kafka.common.serialization.ByteArrayDeserializer"/>
            <unmarshal>
                <protobuf instanceClass="org.opennms.horizon.grpc.heartbeat.contract.HeartbeatMessage"/>
            </unmarshal>
            <process ref="heartbeatProcessor"/>
        </route>
    </camelContext>

</blueprint>
