spring:
  application:
    name: lokahi-minion-certificate-verifier

management:
  endpoints:
    web:
      base-path: "/"
      exposure:
        include: "*"

grpc:
  server:
    deadline: 6000
  url:
    inventory: opennms-inventory:6565
