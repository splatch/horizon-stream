package org.opennms.horizon.flows;

public class Main {
    public static void main(String[] args) {
        /* Main class inserted to avoid the PR checker error:
        Main class was not found, perhaps you should add a `mainClass` configuration to jib-maven-plugin  */
    }
}
