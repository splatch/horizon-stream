<?xml version="1.0" encoding="UTF-8" ?>
<features name="minion-plugin-web-listen-${project.version}" xmlns="http://karaf.apache.org/xmlns/features/v1.6.0">

    <feature name="web-listen-plugin" version="${project.version}">
        <feature version="${jetty.version}">pax-jetty</feature>
        <feature version="${project.version}">plugins-api</feature>
        <bundle>mvn:org.opennms.horizon.minion/WEB-LISTEN/${project.version}</bundle>
    </feature>

</features>
